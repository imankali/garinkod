# shop/urls.py

from django.urls import path
from . import views

app_name = 'shop'

urlpatterns = [
    # این فایل دیگر نیازی به URL های قدیمی ندارد
    # چون همه چیز از طریق api_urls.py مدیریت می‌شود
]