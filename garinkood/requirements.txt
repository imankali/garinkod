# GarinKood backend runtime dependencies
# Python: 3.11+ (tested with Python 3.11)
#
# This file contains only packages required to run the Django API in production.
# Development/test-only tooling belongs in requirements-dev.txt.
# Install with: python -m pip install --upgrade pip && python -m pip install -r requirements.txt

# Core web framework and REST API
Django==5.2.17
# https://www.django-rest-framework.org/
djangorestframework==3.16.1
# Filtering used by the public catalogue and marketplace endpoints
django-filter==25.2
# Same-origin/CORS handling for the frontend proxy
django-cors-headers==4.9.0

# Database and configuration
# PostgreSQL driver used when DB_ENGINE=postgresql.
# The binary distribution keeps local/container installs reproducible.
psycopg2-binary==2.9.12
python-decouple==3.8

# Media uploads and static assets
# Validates/resizes product, comment, visual-search and storefront images.
Pillow==12.3.0
# Production static-file serving; the brotli extra enables compressed responses.
whitenoise[brotli]==6.12.0

# WSGI production server
gunicorn==23.0.0

# Redis client for shared caching and rate-limiting
redis==5.2.1
