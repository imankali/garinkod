# GarinKood development, test and security tooling
# Install with: python -m pip install -r requirements-dev.txt

-r requirements.txt

# ``requests`` is a runtime dependency because official provider adapters use it.

# Test coverage and dependency vulnerability checks
coverage==7.10.6
pip-audit==2.9.0
